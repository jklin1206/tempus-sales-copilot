
.DS_Store
.env
.env.local
.env*.local
.next/
.vercel
.vercel/
*.tsbuildinfo
# build and out of this app's history. Owned by whoever is running Codex.
# Dependencies
# Editor and OS
# Next.js
# Parallel Codex implementation of the same spec. Left on disk, kept out of this
# Secrets. The model key is server-side only and never committed.
codex-prototype/
next-env.d.ts
node_modules/
out/
